# 럭키드로우 경품 추첨 웹사이트 요구사항

## 1. 문서 목적

본 문서는 행사·모임에서 사용할 럭키드로우 경품 추첨 웹사이트의 기능 및 품질 요구사항을 정의한다. 운영자는 참가자와 경품을 입력하고, 진행자는 원판을 돌려 당첨 결과를 공개한다. 당첨자는 경품을 한 번만 수령할 수 있으며, 당첨 또는 꽝 결과가 나온 뒤 원판은 남은 경품 현황에 맞게 다시 구성된다.

이 문서는 구현 방식보다 사용자 경험과 동작 규칙을 우선한다. 화면 문구와 흐름은 과도하게 기술적이거나 기계적으로 보이지 않도록 간결하고 자연스럽게 작성한다.

---

## 2. 서비스 개요

### 2.1 사용 목적

- 사내 행사, 가족 모임, 동호회, 경품 이벤트 등에서 공정하고 이해하기 쉬운 방식으로 경품을 추첨한다.
- 참가자와 경품을 사전에 등록하고, 현장에서 원판 애니메이션을 통해 결과를 발표한다.
- 당첨 처리된 참가자는 다음 추첨 대상에서 자동 제외한다.
- 수령된 경품은 재당첨되지 않도록 원판에서 제거한다.

### 2.2 사용자 역할

| 역할 | 주요 권한 |
| --- | --- |
| 운영자 | 참가자·경품 등록, 추첨 시작, 결과 확인, 초기화, 진행 상태 관리 |
| 진행자 | 추첨 화면에서 원판 회전 및 결과 발표 |
| 참가자 | 화면에 공개된 본인 당첨 여부와 경품 확인 |

초기 버전에서는 별도의 회원가입이나 로그인 없이 하나의 행사 세션을 운영자가 관리하는 형태를 기본으로 한다.

---

## 3. 핵심 운영 규칙

### 3.1 참가자 규칙

- 참가자는 이름 또는 숫자 형태로 입력할 수 있다.
- 예: `김민수`, `1`, `A-12`, `참가자 05`.
- 공백만 있는 값은 등록할 수 없다.
- 동일한 참가자 식별값의 중복 등록은 기본적으로 허용하지 않는다.
- 참가자 수는 최소 1명 이상이어야 추첨을 시작할 수 있다.
- 경품에 당첨된 참가자는 즉시 당첨자 명단으로 이동하며, 남은 추첨 대상에서는 제외한다.
- 꽝 결과가 나온 참가자는 기본적으로 다음 추첨 대상에 남는다. 단, 운영자가 행사 설정에서 “꽝도 탈락 처리”를 켜면 꽝 참가자도 제외할 수 있다.

### 3.2 경품 규칙

- 경품은 이름과 수량으로 등록한다.
- 같은 이름의 경품은 수량을 합산하여 관리할 수 있다.
- 예: `커피 쿠폰 × 5`, `블루투스 이어폰 × 1`.
- 경품 수량은 1 이상의 정수여야 한다.
- 경품이 당첨되면 해당 경품의 남은 수량은 1 감소한다.
- 남은 수량이 0이 된 경품은 이후 원판에서 제거한다.
- 한 참가자는 경품을 최대 1개만 받을 수 있다.

### 3.3 꽝 규칙

- 원판에는 항상 `꽝` 항목이 정확히 한 칸 존재한다.
- 경품이 남아 있는 동안 꽝 칸은 유지된다.
- 모든 경품이 소진되면 추첨을 종료하고 원판을 비활성화한다.
- 꽝은 경품 재고를 차감하지 않는다.
- 꽝이 나온 뒤에도 원판 구성은 동일하게 유지될 수 있으나, 화면은 다음 추첨을 자연스럽게 이어갈 수 있어야 한다.

### 3.4 추첨 단위

한 번의 추첨은 다음 두 단계를 순서대로 수행한다.

1. 현재 추첨 대상 참가자 중 1명을 무작위로 선정한다.
2. 원판을 회전시켜 경품 또는 꽝 결과를 무작위로 선정한다.

결과 화면에는 `당첨자 이름`, `추첨 결과`, `남은 경품 수`를 함께 표시한다.

> 예시: `김민수님, 커피 쿠폰에 당첨되셨습니다!`

---

## 4. 주요 화면 및 기능

### 4.1 행사 설정 화면

운영자는 추첨 시작 전에 참가자와 경품을 준비한다.

#### 참가자 입력

- 단일 입력: 입력창에 이름 또는 숫자를 입력하고 추가한다.
- 일괄 입력: 줄바꿈, 쉼표, 탭으로 구분된 값을 한 번에 등록한다.
- 등록된 참가자 목록을 표시한다.
- 참가자 개수와 중복 또는 빈 값으로 제외된 항목 수를 표시한다.
- 각 참가자를 개별 삭제할 수 있다.
- 전체 삭제 기능을 제공하되, 실행 전 확인 단계를 둔다.

#### 경품 입력

- 경품명과 수량을 입력해 추가한다.
- 일괄 입력을 지원한다. 권장 형식은 `경품명,수량`의 줄 단위 입력이다.
- 등록된 경품의 이름, 초기 수량, 남은 수량을 목록으로 표시한다.
- 경품 수정 및 삭제를 지원한다.
- 이미 추첨이 시작된 뒤에는 경품 수정 시 경고를 표시한다.

#### 행사 옵션

- 꽝도 탈락 처리: 기본값은 꺼짐.
- 추첨 속도: 느리게, 보통, 빠르게 중 선택.
- 결과 공개 효과음: 켜기/끄기.
- 원판 색상 테마: 기본 테마와 고대비 테마 제공.

#### 시작 조건

- 참가자가 1명 이상이고 경품이 1개 이상 등록된 경우에만 `추첨 시작` 버튼을 활성화한다.
- 시작 전 요약 정보를 표시한다.
  - 추첨 대상 인원
  - 총 경품 수량
  - 경품 종류 수
  - 꽝 칸 1개 포함 여부

### 4.2 추첨 진행 화면

이 화면은 현장 프로젝터 또는 큰 모니터에서 보기 쉽게 구성한다.

#### 원판

- 원판은 남은 경품 단위와 꽝 1칸으로 구성한다.
- 경품 수량이 여러 개인 경우, 해당 경품은 수량만큼 여러 조각으로 표현하거나 비율에 맞는 동일 항목 조각으로 표시한다.
- 예: 커피 쿠폰 3개, 영화권 1개, 꽝 1개라면 총 5칸으로 표시한다.
- 각 칸에는 경품명 또는 꽝 문구가 읽을 수 있는 크기로 표시되어야 한다.
- 회전 결과를 가리키는 고정 포인터를 원판 외곽 상단에 배치한다.
- 원판 회전 중에는 중복 클릭이나 추가 추첨을 막는다.

#### 추첨 흐름

1. 진행자가 `추첨하기`를 누른다.
2. 시스템은 현재 대상에서 참가자 1명을 선정하고, 회전 전에는 이름을 공개하지 않는다.
3. 원판이 자연스럽게 감속하며 회전한다.
4. 원판이 멈춘 뒤 결과 모달 또는 결과 영역에 참가자와 결과를 표시한다.
5. 경품 당첨이면 수령 완료 처리 후 해당 참가자와 경품 재고를 갱신한다.
6. 원판은 남은 경품 기준으로 재배치한다.
7. 다음 추첨 버튼을 활성화한다.

#### 결과 표현

- 경품 당첨: 축하 메시지, 경품명, 당첨자 이름을 크게 표시한다.
- 꽝: 위로 문구와 함께 꽝 결과를 표시한다. 지나치게 부정적인 표현은 사용하지 않는다.
- 예시 문구
  - 경품: `축하합니다! 김민수님이 영화 관람권에 당첨되셨습니다.`
  - 꽝: `아쉽지만 이번 결과는 꽝입니다. 다음 기회를 기다려 주세요.`
- 결과 확인 후 운영자가 `다음 추첨`을 누르면 다음 회차로 진행한다.

#### 원판 재배치

- 경품 당첨 후 당첨된 경품 항목의 수량을 1 차감한다.
- 남은 수량이 0인 경품은 원판에서 제거한다.
- 남은 경품과 꽝 1칸으로 원판을 다시 계산하여 렌더링한다.
- 재배치 과정에서 원판이 갑자기 깨지거나 항목이 겹쳐 보이지 않아야 한다.
- 항목 수가 많아 라벨을 모두 표시하기 어려운 경우, 축약 라벨과 범례 목록을 함께 제공한다.

### 4.3 현황 패널

추첨 화면 또는 별도 관리 화면에서 다음 정보를 확인할 수 있어야 한다.

- 남은 추첨 대상 인원
- 전체 경품 잔여 수량
- 경품별 남은 수량
- 현재 회차 번호
- 당첨자 명단
- 꽝 결과 명단
- 아직 당첨되지 않은 참가자 수

당첨자 명단에는 다음 정보를 표시한다.

| 항목 | 설명 |
| --- | --- |
| 회차 | 추첨이 진행된 순서 |
| 당첨자 | 참가자 이름 또는 번호 |
| 경품 | 수령한 경품명 |
| 시각 | 결과가 확정된 시각 |
| 상태 | 수령 완료 |

꽝 결과 명단에는 참가자, 회차, 시각을 표시한다. 꽝도 탈락 처리 옵션이 켜진 경우에는 탈락 상태를 표시한다.

### 4.4 종료 및 결과 관리

- 모든 경품이 소진되면 `추첨이 완료되었습니다` 상태를 표시한다.
- 남은 참가자가 없을 때도 추첨을 종료한다.
- 종료 후 당첨자 명단과 경품 소진 현황을 확인할 수 있어야 한다.
- 결과를 CSV 파일로 내려받을 수 있어야 한다.
- 운영자는 새 행사를 위해 전체 데이터를 초기화할 수 있어야 한다.
- 초기화는 되돌릴 수 없으므로 명확한 확인 문구를 제공한다.

---

## 5. 데이터 모델

### 5.1 참가자

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| id | UUID 또는 숫자 ID | 내부 식별자 |
| displayName | 문자열 | 화면에 표시할 이름 또는 번호 |
| status | 열거형 | `ELIGIBLE`, `WINNER`, `ELIMINATED` |
| createdAt | 일시 | 등록 시각 |
| resultAt | 일시 또는 null | 당첨 또는 탈락 처리 시각 |

### 5.2 경품

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| id | UUID 또는 숫자 ID | 내부 식별자 |
| name | 문자열 | 경품명 |
| initialQuantity | 정수 | 최초 등록 수량 |
| remainingQuantity | 정수 | 현재 남은 수량 |
| createdAt | 일시 | 등록 시각 |

### 5.3 추첨 이력

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| id | UUID 또는 숫자 ID | 이력 식별자 |
| round | 정수 | 추첨 회차 |
| participantId | ID | 선정된 참가자 |
| participantName | 문자열 | 결과 보존용 참가자명 |
| resultType | 열거형 | `PRIZE`, `LOSE` |
| prizeId | ID 또는 null | 경품 결과일 때 경품 ID |
| prizeName | 문자열 또는 null | 경품 결과일 때 경품명 |
| createdAt | 일시 | 결과 확정 시각 |

### 5.4 행사 설정

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| eliminateOnLose | 불리언 | 꽝 참가자 탈락 여부 |
| spinSpeed | 열거형 | `SLOW`, `NORMAL`, `FAST` |
| soundEnabled | 불리언 | 효과음 사용 여부 |
| theme | 문자열 | 화면 테마 |
| status | 열거형 | `SETUP`, `IN_PROGRESS`, `COMPLETED` |

---

## 6. 추첨 로직

### 6.1 참가자 선정

- 상태가 `ELIGIBLE`인 참가자만 후보로 사용한다.
- 후보 배열에서 균등한 확률로 1명을 선택한다.
- 경품 당첨 시 해당 참가자는 `WINNER`로 변경한다.
- 꽝이고 꽝 탈락 옵션이 켜진 경우 해당 참가자는 `ELIMINATED`로 변경한다.
- 꽝이고 옵션이 꺼진 경우 참가자 상태는 `ELIGIBLE`로 유지한다.

### 6.2 원판 결과 선정

- 원판 후보는 `remainingQuantity > 0`인 모든 경품의 남은 수량 단위와 꽝 1개로 구성한다.
- 각 경품의 당첨 확률은 남은 수량에 비례한다.
- 꽝의 확률은 원판 전체 칸 중 정확히 1칸의 비율을 가진다.

경품별 남은 수량의 합을 \(P\)라고 하면, 원판 총 칸 수는 다음과 같다.

\[
T = P + 1
\]

남은 수량이 \(q_i\)인 경품 \(i\)의 당첨 확률은 다음과 같다.

\[
Pr(i) = \frac{q_i}{T}
\]

꽝 확률은 다음과 같다.

\[
Pr(꽝) = \frac{1}{T}
\]

- 무작위 결과는 원판 회전 애니메이션의 종료 위치와 일치해야 한다.
- 애니메이션을 먼저 보여 준 뒤 결과를 임의로 바꾸는 방식은 사용하지 않는다.
- 결과 확정 시점에는 참가자 상태, 경품 재고, 이력을 하나의 작업으로 저장하여 중복 지급을 방지한다.

### 6.3 동시성 및 중복 방지

- 추첨 처리 중에는 추첨 버튼을 비활성화한다.
- 서버가 있는 구조라면 추첨 결과 확정은 서버에서 처리한다.
- 경품 재고 차감과 참가자 상태 변경은 트랜잭션으로 묶는다.
- 동일한 추첨 요청이 네트워크 문제로 재전송되어도 같은 회차가 두 번 반영되지 않도록 요청 식별자(idempotency key)를 사용한다.
- 이미 `WINNER` 상태인 참가자는 어떠한 경우에도 추첨 후보에 포함하지 않는다.

---

## 7. 비기능 요구사항

### 7.1 사용성

- 데스크톱, 태블릿, 모바일 화면에서 사용할 수 있어야 한다.
- 현장 진행을 고려해 추첨 화면의 핵심 텍스트와 버튼은 큰 크기로 제공한다.
- 원판 색상만으로 결과를 구분하지 않고 텍스트와 아이콘 또는 패턴을 함께 사용한다.
- 키보드로도 주요 기능을 조작할 수 있어야 한다.
- 효과음은 사용자가 즉시 켜거나 끌 수 있어야 한다.

### 7.2 성능

- 참가자 1,000명, 경품 단위 500개 수준에서 목록 관리와 추첨 화면이 원활하게 동작해야 한다.
- 추첨 버튼을 누른 뒤 회전 시작까지의 체감 지연은 1초 이내를 목표로 한다.
- 원판 회전 애니메이션은 설정된 속도에 따라 약 2~6초 범위에서 자연스럽게 동작해야 한다.

### 7.3 신뢰성

- 페이지 새로고침 또는 브라우저 재실행 후에도 진행 중인 행사 데이터와 확정된 결과를 복원할 수 있어야 한다.
- 로컬 전용 버전은 브라우저 저장소에 데이터를 보관할 수 있으며, 다중 기기 운영이 필요하면 서버 데이터 저장을 지원한다.
- 결과가 확정되기 전 오류가 발생하면 참가자 제외나 경품 재고 차감이 부분적으로 반영되지 않아야 한다.

### 7.4 보안 및 개인정보

- 참가자 이름은 행사 운영에 필요한 최소 정보만 입력한다.
- 전화번호, 주소 등 민감하거나 불필요한 개인정보 입력을 유도하지 않는다.
- 결과 CSV를 내보낼 때도 등록된 참가자 이름과 추첨 이력 외의 개인정보는 포함하지 않는다.
- 관리 기능을 외부에 공개하는 경우 운영자 접근을 보호하는 인증 또는 행사 관리 코드를 제공한다.

---

## 8. 예외 처리

| 상황 | 처리 방식 |
| --- | --- |
| 참가자 없이 시작 시도 | 참가자를 1명 이상 등록하라는 안내 표시 |
| 경품 없이 시작 시도 | 경품을 1개 이상 등록하라는 안내 표시 |
| 모든 경품 소진 | 추첨 종료 상태로 전환하고 원판 비활성화 |
| 추첨 대상 없음 | 추첨 종료 상태로 전환하고 사유 표시 |
| 중복 참가자 입력 | 중복 항목임을 표시하고 추가하지 않음 |
| 경품 수량이 0 또는 음수 | 저장하지 않고 유효한 수량 입력 요청 |
| 추첨 중 새로고침 | 마지막 확정 결과를 복원하고 진행 중 상태를 안전하게 정리 |
| 추첨 요청 중복 발생 | 동일 요청은 한 번만 결과로 확정 |
| 긴 경품명 | 원판에서는 축약 표시하고 전체 이름은 범례·결과에서 표시 |

---

## 9. 완료 기준

다음 조건을 모두 만족하면 초기 버전을 완료한 것으로 본다.

- 운영자가 이름 또는 숫자로 참가자를 개별·일괄 등록할 수 있다.
- 운영자가 경품명과 수량을 개별·일괄 등록할 수 있다.
- 원판에 남은 경품 수량 단위와 꽝 1칸이 정확히 반영된다.
- 추첨 시 대상 참가자와 원판 결과가 무작위로 결정된다.
- 경품 당첨자는 이후 추첨 후보에서 제외된다.
- 당첨 경품의 재고가 1 감소하며, 재고가 0인 경품은 원판에서 사라진다.
- 꽝은 경품이 남아 있는 동안 정확히 한 칸으로 유지된다.
- 추첨 결과와 당첨자 명단이 화면에 누적된다.
- 새로고침 뒤에도 확정된 결과가 유지된다.
- 모든 경품이 소진되면 더 이상 추첨할 수 없다.
- 결과를 CSV로 내보낼 수 있고, 운영자가 행사 데이터를 초기화할 수 있다.

---

## 10. 향후 확장 항목

초기 버전의 범위에는 포함하지 않지만, 필요 시 다음 기능을 추가할 수 있다.

- QR 코드로 참가자가 직접 이름을 등록하는 접수 화면
- 여러 행사 세션 및 과거 행사 결과 관리
- 운영자와 진행자 역할 분리
- 경품별 고정 당첨 확률 또는 가중치 설정
- 특정 참가자 그룹별 추첨
- 당첨자 화면을 별도 모니터로 송출하는 디스플레이 모드
- 사내 SSO 또는 관리자 계정 연동
- 추첨 로그 감사 기능과 결과 서명
- 다국어 화면 지원
